pipeline {
    agent any
    stages {
       stage('Clone git') { 
            steps { sh 'rm -r fyp'
                    sh 'mkdir fyp'
                    sh 'cd fyp'
                    dir ('fyp') {
                    git url : 'https://github.com/20022717-Muhammad-Irfan/fyp.git', branch: 'main'
                    }
                }
                }
        stage('Build and Deploy') {
            steps {
               sh 'echo "fyp2022" | sudo -S docker build -t myimage .'
               sh 'echo "fyp2022" | sudo -S docker run -d -it -h fyp --name FYP myimage /bin/bash'
               sh 'echo "fyp2022" | sudo -S docker stop FYP '
               sh 'echo "fyp2022" | sudo -S docker connect --ip 172.18.0.3 docker-network FYP'
               sh 'echo "fyp2022" | sudo -S docker start FYP '
            }
        }
        stage('Test') {
            
            parallel {
            
                stage('Docker Container Test') {
                  
                   steps {
                       echo 'build container'
                    }
                }
                stage('End-to-End Test') {
                    
                    steps {
                        echo 'User testing the web application'
                    }
                }
                stage('Web App Test') {
                    
                    steps {
                       echo 'Testing the features are working'
                    }
                }
            }
        }
        
    }
}
