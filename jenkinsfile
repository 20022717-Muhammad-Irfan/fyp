pipeline {
    agent { dockerfile true}
    stages {
       stage('Clone git') { 
            steps { sh 'mkdir fyp'
                    sh 'cd fyp'
                    dir ('fyp') {
                    git url : 'https://github.com/20022717-Muhammad-Irfan/fyp.git', branch: 'main'
                    }
                }
                }
        stage('Build and Deploy') {
            steps {
               sh 'echo "docker&8" | docker build -t myimage .'
               sh 'echo "docker&8" | docker run -d -it -h fyp --name FYP myimage /bin/bash'
               sh 'echo "docker&8" | docker start FYP '
            }
        }
        stage('Test') {
            
            parallel {
            
                stage('Docker Container Test') {
                  
                   steps {
                       echo 'build container'
                    }
                }
                stage('End-to-End Test') {
                    
                    steps {
                        echo 'User testing the web application'
                    }
                }
                stage('Web App Test') {
                    
                    steps {
                       echo 'Testing the features are working'
                    }
                }
            }
        }
        
    }
}
