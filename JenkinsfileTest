pipeline {
    agent any

    stages {
        stage('run-parallel-branches') {
            steps { 
              parallel(
                a: {
                  echo "test 1.0"
                  },
                b: {
                echo "test 2.0"
                
            }
            )
            }
        }
    
